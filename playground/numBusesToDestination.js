function numBusesToDestination(routes, source, target) {
    // write code here
    console.log(routes, source, target);

    if (source === target) {
        return 0;
    }

    for (let i = 0; i < routes.le; i++) {
        // const element = array[i];
    }
}

numBusesToDestination(
    [
        [1, 2, 7],
        [3, 6, 7],
    ],
    1,
    6
);
